#include <stdio.h>

int main() {
    //sum of n odd numbers
    int n;
    printf("inserta el n numero impar: ");
    scanf("%d", &n);
    int sum = n*n;
    printf("la suma de los %d primeros numeros impares es: %d", n, sum);

}